#!/usr/bin/env python3
"""Run reproducible Kipp throughput and latency benchmarks.

The benchmark harness is tooling only and will execute the native engine as a
separate process once a backend exists.
"""


def main() -> None:
    raise SystemExit("Kipp benchmarks are unavailable until inference is implemented.")


if __name__ == "__main__":
    main()
