# Kipp — Claude Code Notes

Read `AGENT.md` first — it is the source of truth and applies to every agent
working in this repository, not just Claude Code. Then read
`.claude/context.md` for a Claude-specific briefing on the current project
state (Cursor's equivalent lives in `.cursor/rules/`).

Do not commit, open a pull request, set a remote, or push unless the user asks
explicitly. State which hardware backends were actually tested.
